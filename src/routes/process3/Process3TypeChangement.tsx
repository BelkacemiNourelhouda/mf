import Process3TypeChangement from '@/components/Process3/Process3TypeChangement'
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('Process3TypeChangement')({
  component:()=> <Process3TypeChangement/>,
})
